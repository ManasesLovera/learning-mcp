[project]
name = "generic-fastmcp-server"
version = "0.1.0"
description = "Generic FastMCP server with 5 sample tools and HTTP transport"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.13,<4",
]

[tool.uv]
package = false
